# Hooks Module

## Purpose
Global reusable React custom hooks (e.g. screen resize, prefers reduced motion, viewport dimensions, animation state).

## Ownership
- **Owner**: Frontend Engineer

## Dependencies
React, `@/stores`

## Public API
Custom React Hooks (`useResize`, `usePrefersReducedMotion`)
