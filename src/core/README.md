# Core Module

## Purpose
Global configurations, constants, systems bootstrap, and core business/library configuration setups.

## Ownership
- **Owner**: Technical Architect

## Dependencies
gsap, animejs, lenis

## Public API
`animation.ts` (Animation configs), `gsap.ts` (GSAP global configuration), `lenis.ts` (Scroll configs)
