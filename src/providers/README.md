# Providers Module

## Purpose
Core React Context providers controlling Theme, Accessibility, Render settings, Interaction parameters, and overall Experience State.

## Ownership
- **Owner**: Technical Architect

## Dependencies
React, `@/stores`

## Public API
`ThemeProvider`, `AccessibilityProvider`, `ExperienceProvider`, `RenderProvider`, `InteractionProvider`
